# Toonflow Data Model (db2.sqlite)

- DB file: `data/db2.sqlite` (knex + better-sqlite3) — **gitignored runtime user data**
- Schema + seed data source: `src/lib/initDB.ts`; boot-time migrations/repairs: `src/lib/fixDB.ts`
- Table TS types: `src/types/database.d.ts` (auto-generated by `@rmp135/sql-ts` in dev)
- Generated by `yarn docs:data-model`. Do not edit by hand.

## Tables (24)

| Table | Purpose | Rows |
|---|---|---|
| `o_agentDeploy` | Per-agent-key AI model deployment: `key` = agent key (`scriptAgent`, `productionAgent`, `universalAi`, `scriptAgent:decisionAgent`, ...), `modelName` = `<vendorId>:<modelName>`, plus temperature/maxOutputTokens/disabled/type. | 17 |
| `o_agentWorkData` | Agent workspace/plan data: `projectId` + `episodesId` + `key` (storySkeleton, adaptationStrategy, scriptPlan, storyboardTable, ...) → `data` JSON string. | 0 |
| `o_artStyle` | Art-style presets (name, label, prompt, preview image fileUrl in OSS). | 0 |
| `o_assets` | Asset center entries (derived assets, storyboard assets, character/scene/role images). Links to `o_image` via `imageId`; `promptState` tracks prompt generation. | 0 |
| `o_assets2Storyboard` | Join: storyboard panel ↔ asset (a panel may reuse existing assets). | 0 |
| `o_event` | Extracted chapter events (AI-generated from novel text). | 0 |
| `o_eventChapter` | Join: event ↔ novel chapter (`eventId`, `novelId`). | 0 |
| `o_image` | Image records. `filePath` → OSS, `state` = 生成中/成功/失败, `model`/`resolution`, `errorReason`. | 0 |
| `o_imageFlow` | Image-flow editor graphs (JSON node graph in `flowData`). | 0 |
| `o_modelPrompt` | Binding of an image/video model to a video-prompt template file under `data/modelPrompt/video/*.md`. | 0 |
| `o_novel` | Novel chapters. `chapterData` = raw novel text, `event`/`eventState` = extraction result from `u.cleanNovel`. | 0 |
| `o_project` | Project entity (a novel adaptation). Holds projectType, novel info, chosen image/video models, artStyle, directorManual, videoRatio, mode. | 0 |
| `o_prompt` | Reusable prompt templates (`data`, `useData`). | 4 |
| `o_script` | Generated scripts. `content` = the `<scriptItem>` XML; `extractState` tracks asset extraction. | 0 |
| `o_scriptAssets` | Join: script ↔ asset (`scriptId`, `assetId`). | 0 |
| `o_setting` | App-wide key/value settings: `tokenKey` (JWT secret), `agentUseMode` (0 simple / 1 advanced), memory tuning, `switchAiDevTool`, etc. | 11 |
| `o_skillAttribution` | Skill ↔ attribution binding (`skillId`, `attribution` = project/agent scope). | 14 |
| `o_skillList` | Installed skills registry: name, description, path, md5, embedding (for retrieval), state. | 21 |
| `o_storyboard` | Storyboard panels: prompt, filePath (OSS image), state, track/trackId, `shouldGenerateImage`, videoDesc. Also the derive-asset lineage via `assetsId`-style links. | 0 |
| `o_tasks` | AI generation task records. `taskClass` groups tasks, `relatedObjects` lists affected ids, `state` = 进行中/成功/失败 (in-flight rows are filtered out of polling endpoints). | 0 |
| `o_user` | Login accounts. Seeded with a default admin (see `src/lib/initDB.ts`). | 1 |
| `o_vendorConfig` | Vendor (LLM provider) config: `id` = vendor id, `inputValues` = baseURL/apiKey/etc, `models` = available models JSON, `enable` flag. Vendor template code lives on disk in `data/vendor/<id>.ts`. | 10 |
| `o_video` | Generated video records (workbench): filePath, state, videoTrackId, errorReason. | 0 |
| `o_videoTrack` | Video timeline tracks on the workbench: prompt, duration, selectVideoId, state. | 0 |

## `o_agentDeploy`

Per-agent-key AI model deployment: `key` = agent key (`scriptAgent`, `productionAgent`, `universalAi`, `scriptAgent:decisionAgent`, ...), `modelName` = `<vendorId>:<modelName>`, plus temperature/maxOutputTokens/disabled/type.

| Col | Type | PK |
|---|---|---|
| `id` | INTEGER | ✅ |
| `model` | varchar(255) |  |
| `key` | varchar(255) |  |
| `modelName` | varchar(255) |  |
| `vendorId` | TEXT |  |
| `desc` | varchar(255) |  |
| `name` | varchar(255) |  |
| `temperature` | INTEGER |  |
| `maxOutputTokens` | INTEGER |  |
| `disabled` | boolean |  |
| `type` | varchar(255) |  |

Indexes:
- `o_agentdeploy_id_unique` (unique) on `id`

## `o_agentWorkData`

Agent workspace/plan data: `projectId` + `episodesId` + `key` (storySkeleton, adaptationStrategy, scriptPlan, storyboardTable, ...) → `data` JSON string.

| Col | Type | PK |
|---|---|---|
| `id` | INTEGER | ✅ |
| `projectId` | INTEGER |  |
| `episodesId` | INTEGER |  |
| `key` | varchar(255) |  |
| `data` | varchar(255) |  |
| `createTime` | INTEGER |  |
| `updateTime` | INTEGER |  |

Indexes:
- `o_agentworkdata_id_unique` (unique) on `id`

## `o_artStyle`

Art-style presets (name, label, prompt, preview image fileUrl in OSS).

| Col | Type | PK |
|---|---|---|
| `id` | INTEGER | ✅ |
| `name` | varchar(255) |  |
| `fileUrl` | TEXT |  |
| `label` | TEXT |  |
| `prompt` | TEXT |  |

Indexes:
- `o_artstyle_id_unique` (unique) on `id`

## `o_assets`

Asset center entries (derived assets, storyboard assets, character/scene/role images). Links to `o_image` via `imageId`; `promptState` tracks prompt generation.

| Col | Type | PK |
|---|---|---|
| `id` | INTEGER | ✅ |
| `name` | TEXT |  |
| `prompt` | TEXT |  |
| `remark` | TEXT |  |
| `type` | TEXT |  |
| `describe` | TEXT |  |
| `scriptId` | INTEGER |  |
| `imageId` | INTEGER |  |
| `assetsId` | INTEGER |  |
| `projectId` | INTEGER |  |
| `flowId` | INTEGER |  |
| `startTime` | INTEGER |  |
| `promptState` | varchar(255) |  |
| `audioBindState` | INTEGER |  |
| `promptErrorReason` | TEXT |  |

Indexes:
- `o_assets_id_unique` (unique) on `id`

## `o_assets2Storyboard`

Join: storyboard panel ↔ asset (a panel may reuse existing assets).

| Col | Type | PK |
|---|---|---|
| `storyboardId` | INTEGER | ✅ |
| `assetId` | INTEGER | ✅ |

Indexes:
- `o_assets2storyboard_storyboardid_assetid_unique` (unique) on `storyboardId`, `assetId`

## `o_event`

Extracted chapter events (AI-generated from novel text).

| Col | Type | PK |
|---|---|---|
| `id` | INTEGER | ✅ |
| `name` | varchar(255) |  |
| `detail` | varchar(255) |  |
| `createTime` | INTEGER |  |

Indexes:
- `o_event_id_unique` (unique) on `id`

## `o_eventChapter`

Join: event ↔ novel chapter (`eventId`, `novelId`).

| Col | Type | PK |
|---|---|---|
| `id` | INTEGER | ✅ |
| `eventId` | INTEGER |  |
| `novelId` | INTEGER |  |

Indexes:
- `o_eventchapter_id_unique` (unique) on `id`

## `o_image`

Image records. `filePath` → OSS, `state` = 生成中/成功/失败, `model`/`resolution`, `errorReason`.

| Col | Type | PK |
|---|---|---|
| `id` | INTEGER | ✅ |
| `filePath` | TEXT |  |
| `type` | TEXT |  |
| `assetsId` | INTEGER |  |
| `model` | TEXT |  |
| `resolution` | TEXT |  |
| `state` | TEXT |  |
| `errorReason` | TEXT |  |

Indexes:
- `o_image_id_unique` (unique) on `id`

## `o_imageFlow`

Image-flow editor graphs (JSON node graph in `flowData`).

| Col | Type | PK |
|---|---|---|
| `id` | INTEGER | ✅ |
| `flowData` | TEXT |  |

Indexes:
- `o_imageflow_id_unique` (unique) on `id`

## `o_modelPrompt`

Binding of an image/video model to a video-prompt template file under `data/modelPrompt/video/*.md`.

| Col | Type | PK |
|---|---|---|
| `id` | INTEGER | ✅ |
| `vendorId` | varchar(255) |  |
| `model` | varchar(255) |  |
| `fileName` | TEXT |  |
| `path` | TEXT |  |

Indexes:
- `o_modelprompt_id_unique` (unique) on `id`

## `o_novel`

Novel chapters. `chapterData` = raw novel text, `event`/`eventState` = extraction result from `u.cleanNovel`.

| Col | Type | PK |
|---|---|---|
| `id` | INTEGER | ✅ |
| `chapterIndex` | INTEGER |  |
| `reel` | TEXT |  |
| `chapter` | TEXT |  |
| `chapterData` | TEXT |  |
| `projectId` | INTEGER |  |
| `eventState` | INTEGER |  |
| `event` | TEXT |  |
| `errorReason` | TEXT |  |
| `createTime` | INTEGER |  |

Indexes:
- `o_novel_id_unique` (unique) on `id`

## `o_project`

Project entity (a novel adaptation). Holds projectType, novel info, chosen image/video models, artStyle, directorManual, videoRatio, mode.

| Col | Type | PK |
|---|---|---|
| `id` | INTEGER | ✅ |
| `projectType` | varchar(255) |  |
| `imageModel` | varchar(255) |  |
| `imageQuality` | varchar(255) |  |
| `videoModel` | varchar(255) |  |
| `name` | TEXT |  |
| `intro` | TEXT |  |
| `type` | TEXT |  |
| `artStyle` | TEXT |  |
| `directorManual` | TEXT |  |
| `mode` | TEXT |  |
| `videoRatio` | TEXT |  |
| `createTime` | INTEGER |  |
| `userId` | INTEGER |  |

Indexes:
- `o_project_id_unique` (unique) on `id`

## `o_prompt`

Reusable prompt templates (`data`, `useData`).

| Col | Type | PK |
|---|---|---|
| `id` | INTEGER | ✅ |
| `name` | varchar(255) |  |
| `type` | varchar(255) |  |
| `data` | TEXT |  |
| `useData` | TEXT |  |

Indexes:
- `o_prompt_id_unique` (unique) on `id`

## `o_script`

Generated scripts. `content` = the `<scriptItem>` XML; `extractState` tracks asset extraction.

| Col | Type | PK |
|---|---|---|
| `id` | INTEGER | ✅ |
| `name` | TEXT |  |
| `content` | TEXT |  |
| `projectId` | INTEGER |  |
| `extractState` | INTEGER |  |
| `createTime` | INTEGER |  |
| `errorReason` | TEXT |  |

Indexes:
- `o_script_id_unique` (unique) on `id`

## `o_scriptAssets`

Join: script ↔ asset (`scriptId`, `assetId`).

| Col | Type | PK |
|---|---|---|
| `scriptId` | INTEGER | ✅ |
| `assetId` | INTEGER | ✅ |

Indexes:
- `o_scriptassets_scriptid_assetid_unique` (unique) on `scriptId`, `assetId`

## `o_setting`

App-wide key/value settings: `tokenKey` (JWT secret), `agentUseMode` (0 simple / 1 advanced), memory tuning, `switchAiDevTool`, etc.

| Col | Type | PK |
|---|---|---|
| `key` | TEXT | ✅ |
| `value` | TEXT |  |

Indexes:
- `o_setting_key_unique` (unique) on `key`

## `o_skillAttribution`

Skill ↔ attribution binding (`skillId`, `attribution` = project/agent scope).

| Col | Type | PK |
|---|---|---|
| `skillId` | TEXT | ✅ |
| `attribution` | TEXT | ✅ |

Indexes:
- `o_skillattribution_attribution_index` on `attribution`

## `o_skillList`

Installed skills registry: name, description, path, md5, embedding (for retrieval), state.

| Col | Type | PK |
|---|---|---|
| `id` | TEXT | ✅ |
| `md5` | TEXT |  |
| `path` | TEXT |  |
| `name` | TEXT |  |
| `description` | TEXT |  |
| `embedding` | TEXT |  |
| `type` | TEXT |  |
| `createTime` | INTEGER |  |
| `updateTime` | INTEGER |  |
| `state` | INTEGER |  |

## `o_storyboard`

Storyboard panels: prompt, filePath (OSS image), state, track/trackId, `shouldGenerateImage`, videoDesc. Also the derive-asset lineage via `assetsId`-style links.

| Col | Type | PK |
|---|---|---|
| `id` | INTEGER | ✅ |
| `scriptId` | INTEGER |  |
| `prompt` | TEXT |  |
| `filePath` | TEXT |  |
| `duration` | TEXT |  |
| `state` | TEXT |  |
| `trackId` | INTEGER |  |
| `reason` | TEXT |  |
| `track` | TEXT |  |
| `videoDesc` | TEXT |  |
| `shouldGenerateImage` | INTEGER |  |
| `projectId` | INTEGER |  |
| `flowId` | INTEGER |  |
| `index` | INTEGER |  |
| `createTime` | INTEGER |  |

Indexes:
- `o_storyboard_id_unique` (unique) on `id`

## `o_tasks`

AI generation task records. `taskClass` groups tasks, `relatedObjects` lists affected ids, `state` = 进行中/成功/失败 (in-flight rows are filtered out of polling endpoints).

| Col | Type | PK |
|---|---|---|
| `id` | INTEGER | ✅ |
| `projectId` | INTEGER |  |
| `taskClass` | varchar(255) |  |
| `relatedObjects` | varchar(255) |  |
| `model` | varchar(255) |  |
| `describe` | TEXT |  |
| `state` | varchar(255) |  |
| `startTime` | INTEGER |  |
| `reason` | TEXT |  |

Indexes:
- `o_tasks_id_unique` (unique) on `id`

## `o_user`

Login accounts. Seeded with a default admin (see `src/lib/initDB.ts`).

| Col | Type | PK |
|---|---|---|
| `id` | INTEGER | ✅ |
| `name` | TEXT |  |
| `password` | TEXT |  |

Indexes:
- `o_user_id_unique` (unique) on `id`

## `o_vendorConfig`

Vendor (LLM provider) config: `id` = vendor id, `inputValues` = baseURL/apiKey/etc, `models` = available models JSON, `enable` flag. Vendor template code lives on disk in `data/vendor/<id>.ts`.

| Col | Type | PK |
|---|---|---|
| `id` | varchar(255) | ✅ |
| `inputValues` | TEXT |  |
| `models` | TEXT |  |
| `enable` | INTEGER |  |

Indexes:
- `o_vendorconfig_id_unique` (unique) on `id`

## `o_video`

Generated video records (workbench): filePath, state, videoTrackId, errorReason.

| Col | Type | PK |
|---|---|---|
| `id` | INTEGER | ✅ |
| `filePath` | TEXT |  |
| `errorReason` | TEXT |  |
| `time` | INTEGER |  |
| `state` | TEXT |  |
| `scriptId` | INTEGER |  |
| `projectId` | INTEGER |  |
| `videoTrackId` | INTEGER |  |

Indexes:
- `o_video_id_unique` (unique) on `id`

## `o_videoTrack`

Video timeline tracks on the workbench: prompt, duration, selectVideoId, state.

| Col | Type | PK |
|---|---|---|
| `id` | INTEGER | ✅ |
| `videoId` | INTEGER |  |
| `projectId` | INTEGER |  |
| `scriptId` | INTEGER |  |
| `state` | TEXT |  |
| `reason` | TEXT |  |
| `prompt` | TEXT |  |
| `selectVideoId` | INTEGER |  |
| `duration` | INTEGER |  |

Indexes:
- `o_videotrack_id_unique` (unique) on `id`